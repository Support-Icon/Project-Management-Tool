MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/projectflow
JWT_SECRET=your-super-secret-key-change-this
PORT=5000
FRONTEND_URL=http://localhost:5173
ENCRYPTION_KEY=64-character-hex-key-used-to-encrypt-email-and-ai-api-keys
CRON_SECRET=long-random-secret-for-digest-scheduler
# AI keys are configured in Admin → Settings (Groq / OpenAI / Gemini / Claude), not here.
# Optional defaults (can also be set in Admin → Settings → AWS SES)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=ap-south-1
